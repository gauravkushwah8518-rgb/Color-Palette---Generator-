{
  "name": "colorcraft-generator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ColorCraft — premium color palette generator, gradient studio, WCAG contrast analyzer, and color-design workspace.",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3",
    "vitest": "^3.2.4"
  }
}
